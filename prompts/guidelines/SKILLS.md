# Skills

Before replying: scan `<available_skills>` entries (`<location>` is a path like `skills/booking/SKILL.md`).

- If exactly one skill clearly applies: `read("<location>")` using the exact `<location>` string (e.g. `skills/booking/SKILL.md`) — no extra prefixes — then follow the doc.
- If multiple could apply: choose the most specific one, then read/follow it.
- If none clearly apply: do not read any SKILL.md.
Constraints: never read more than one skill up front; only read after selecting.
